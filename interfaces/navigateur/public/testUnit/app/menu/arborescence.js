require(['arborescence'], function (Arborescence) {

  /*  module('Arborescence',{
        setup: function() {
            this.menu = new Arborescence();
        }
    });
    
    test('constructor ', function() {
        strictEqual(this.menu._panel.title, 'Arborescence des couches', 'succeed !');
        strictEqual(this.menu._panel.id, 'tree_panel', 'succeed !');
    });
    
    test('obtenirId ', function() {
        strictEqual(this.menu.obtenirId(), 'tree_panel', 'succeed !');
    });
    
    test('_getPanel ', function() {
        strictEqual(this.menu._getPanel().title, 'Arborescence des couches', 'succeed !');
        strictEqual(this.menu._getPanel().id, 'tree_panel', 'succeed !');
    });
    */
});